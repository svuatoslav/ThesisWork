using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ewf : MonoBehaviour
{
    //[SerializeField] GameObject El0 = null;
    //[SerializeField] GameObject El1 = null;
    //[SerializeField] GameObject El2 = null;
    //[SerializeField] GameObject Washer = null;
    private void Start()
    {
        Debug.Log("!");
        //Instantiate(gameObject);
        //El0.transform.position = new Vector3(0f, 0f, 25f);
        //El1.transform.position = new Vector3(1f, 0f, 25f);
        //El2.transform.position = new Vector3(1/2f, Mathf.Sqrt(3)/2, 25f);
        //Washer.transform.position = new Vector3(1/2f, Mathf.Sqrt(3)/2, 24.5f);
        //Washer.AddComponent<HingeJoint>();
    }
}
